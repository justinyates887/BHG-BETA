const bhconfig = require("../core/bhconfig.json");
const fs = require("fs");
const Discord = require("discord.js");
const mongo = require('../../mongo');

module.exports = {
    name: 'msg-counter',
    description: 'counts amount of messages a user has sent per server',

    execute(client, msg, args, logs, blueLogs){
        

    }
}
